abstract class AppDuration {
  static const Duration absorptionPointerDuration =
      Duration(milliseconds: 2600);

  static const Duration snackbarDuration = Duration(seconds: 2);
  static const Duration startCounterDuration = Duration(milliseconds: 200);
  static const Duration stopQuizbsorption = Duration(milliseconds: 6500);
  static const Duration firstQuestionDuration = Duration(milliseconds: 66500);
  static const Duration fillTimerBoxAndNextQuestionDuration =
      Duration(seconds: 60);
  static const Duration delayOfStartAnimationDuration =
      Duration(milliseconds: 6500);
  static const Duration fillResultBoxDuration = Duration(seconds: 1);

  static const Duration navigateToResultDuration = Duration(seconds: 3);
  static const Duration animatedOpacity = Duration(seconds: 3);
}
